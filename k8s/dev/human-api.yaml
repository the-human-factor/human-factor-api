apiVersion: apps/v1
kind: Deployment
metadata:
  name: human-api-deployment
  labels:
    app: human-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: human-api
  template:
    metadata:
      labels:
        app: human-api
    spec:
      containers:
        - name: human-api
          image: gcr.io/the-human-factor/human-api:master
          ports:
            - containerPort: 9000
          volumeMounts:
            - name: credentials
              mountPath: "/credentials"
              readOnly: true
          env:
            - name: FLASK_ENV
              value: staging
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /credentials/google-credentials.json
            - name: FLASK_DB_HOST
              value: human-api-db-postgresql.development.svc.cluster.local
            - name: FLASK_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: human-api-db-postgresql
                  key: postgresql-password
<<<<<<< HEAD
            - name: FLASK_JWT_SECRET_KEY
=======
            - name: JWT_SECRET_KEY
>>>>>>> 0dd1531... Get auth code running with jwt
              valueFrom:
                secretKeyRef:
                  name: human-api-jwt-secret
                  key: secret

      volumes:
        - name: credentials
          secret:
            secretName: google-credentials
            items:
              - key: google-credentials.json
                path: google-credentials.json
---
apiVersion: v1
kind: Service
metadata:
  name: human-api-service
spec:
  type: NodePort
  selector:
    app: human-api
  ports:
    - port: 443
      targetPort: 9000
      protocol: TCP
